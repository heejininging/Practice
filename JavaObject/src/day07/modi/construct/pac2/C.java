package day07.modi.construct.pac2;

import day07.modi.construct.pac1.A;

public class C {

	A a = new A(10);
//	A b = new A(true); //A의 생성자 중 아무것도 붙어있지않은 default는 다른 패키지에서 사용불가능
//	A c = new A("a"); //A의 생성자 중 private가 붙어있으면 다른 클래스에서 사용 불가능

	
}

